type Query {
  product(id: String): Product
}

type Product {
  id: ID!
  name: String!
  brand: Brand!
  url: String
  image_url: String
  reviewer_average: Float!
  review_count: Int!
}

type Brand {
  id: ID!
  url: String
  name: String!
}
